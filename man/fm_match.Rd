% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fm_match.R
\name{fm_match}
\alias{fm_match}
\title{Fit a Pairwise Functional Map}
\usage{
fm_match(
  source,
  target,
  descriptors,
  penalties = list(descr = 0.1, lap = 0.001, comm = 1),
  init = c("zeros", "identity", "random"),
  maxit = 200,
  optimizer = c("cg", "lbfgsb"),
  cg_tol = 1e-06,
  cg_maxit = NULL,
  kernel_backend = c("auto", "r", "cpp"),
  descriptor_batch_size = NULL,
  compute_objective = TRUE,
  trace = FALSE
)
}
\arguments{
\item{source}{Source `fm_domain` with basis and descriptors.}

\item{target}{Target `fm_domain` with basis and descriptors.}

\item{descriptors}{List with `source` and `target` descriptor matrices.}

\item{penalties}{Named list with weights for `descr`, `lap`, and `comm`.}

\item{init}{Initialization strategy (`"zeros"`, `"identity"`, `"random"`).}

\item{maxit}{Maximum optimization iterations.}

\item{optimizer}{Optimization backend (`"cg"` or `"lbfgsb"`).}

\item{cg_tol}{Residual tolerance used by conjugate-gradient solver.}

\item{cg_maxit}{Optional maximum conjugate-gradient iterations.}

\item{kernel_backend}{Kernel backend for objective/gradient ops:
`"auto"` (prefer compiled), `"r"`, or `"cpp"`.}

\item{descriptor_batch_size}{Optional descriptor chunk size for commutativity
operator streaming. Use smaller values to reduce peak memory when many
descriptors are used.}

\item{compute_objective}{Whether to compute the final objective breakdown
after fitting. Disable to skip post-hoc diagnostics when only the map is
needed.}

\item{trace}{Whether to print optimizer traces.}
}
\value{
An `fm_fit` object with map, diagnostics, and objective breakdown.
}
\description{
Fit a Pairwise Functional Map
}
